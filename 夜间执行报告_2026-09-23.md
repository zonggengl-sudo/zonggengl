# 夜间执行报告 2026-09-23

> HY4 夜间免费时段批处理（23:00–08:00）｜首次执行
> 工作目录：`/Users/lizonggeng/WorkBuddy/2026-09-19-07-33-49/kyniqo夜间执行任务/`

---

## 0. 前置条件检测

| 前置项 | 状态 | 说明 |
|---|---|---|
| 飞书（李宗耕） | ✅ 可用 | user token 显示 `needs_refresh`，refresh token 有效期至 2026-09-25，自动续期成功 |
| 飞书 proxy | ⚠️ 需绕过 | 环境 `HTTP_PROXY=127.0.0.1:1082` 无监听，所有飞书调用必须 `env -u HTTP_PROXY ...`（已内置到脚本） |
| 飞书 scope | ⚠️ 部分缺失 | `search:docs:read`、`space:document:retrieve` 未授权 → 无法用搜索/列举云盘；改用 wiki 节点遍历定位表格（已可行） |
| Apify API Key | ❌ 未配置 | 环境/`~/.zshrc`/`~/.apify`/mcp.json 均无 token，抓取类子任务跳过 |
| git remote | ✅ 已配置 | 本地仓库已 `git init`，remote = `https://github.com/zonggengl-sudo/zonggengl.git` |
| GitHub 凭证 | ✅ 已连接 | 账号 `zonggengl-sudo`，仓库 `zonggengl`（**仓库名待你确认**） |
| 邮件 | ⏸ 未发送 | 夜间不外发（符合授权边界）。联系人已记录：`kyniqo.official@gmail.com`、`pr@kyniqo.com` |

---

## 1. Nex Playground 数据抓取（原清单5）— 部分完成

**目标**：Meta 广告 / 独立站 / Facebook 主页三类数据 → 飞书多维表格

| 数据源 | 结果 | 说明 |
|---|---|---|
| **独立站内容** | ✅ 完成 | 自研爬虫抓取 **83 个营销页**（83/83 成功），新增 **27 条**到飞书「独立站内容」表 |
| **独立站商品** | ✅ 完成 | Shopify 公开 JSON，6 个 SKU + 3 个集合，存本地 |
| **Meta 广告** | ⏸ 跳过 | 无 Apify Key。已实测 `facebook.com/ads/library` 纯 JS 渲染，直连只返回 "Ad Library" 空壳，必须有渲染型抓取 |
| **Facebook 主页** | ⏸ 跳过 | 无 Apify Key，且主页内容需登录态 |

### 独立站内容详情
- 从 `nexplayground.com/sitemap.xml`（964 URL）筛掉 blog 与 4 个 locale 子树，得到 83 个英文营销页
- 逐页提取 `<title>` + H1/H2/H3 + 正文段落，按文档顺序输出为 `[H2] 标题\n段落` 格式，与表内既有记录格式一致
- 与飞书已有 56 条按 URL 去重，按路径映射到表的既有 select 选项（游戏详情 / 购买页面 / 订阅服务 / 产品介绍 / 用户故事）
- 新增分布：游戏详情 21、购买页面 4、产品介绍 1、用户故事 1
- 12 个页面因无匹配分类未写入：`/about`、`/careers`、`/contact`、`/privacy`、`/trust`、`/mission`、`/refund` 等（建议后续给表补「品牌/法务」类目再纳入）

**产出文件**
- `data/nex_playground/site_pages.json`（83 页全文，1.1MB）
- `data/nex_playground/products.json`、`collections.json`、`site_summary.json`
- `data/nex_playground/sitemap.xml`
- `tools/crawl_nex_site.py`、`tools/fetch_shopify_site.py`

---

## 2. Shopify blog post 上线时间登记（原清单6）— 完成 ✅

**重要发现**：本任务的自有站点是 **kyniqo.com**（KYNIQO 品牌站），不是 nexplayground.com（后者是竞品）。已按 kyniqo.com 执行。

- 从 `kyniqo.com/sitemap_blogs_1.xml` 取到 **30 篇文章**
- 逐篇解析 JSON-LD `datePublished` / `dateModified`，30/30 成功
- 与飞书「Shopify文章统计（blog posts）> 文章统计」30 条记录比对，**30/30 匹配**，修正 **24 条**上线时间，6 条原本已准确
- 修正后再次 dry-run，差异为 **0**

### 顺带发现的两个数据质量问题
1. **链接单元格损坏**：表内 markdown 链接的 target 一律指向 `https://kyniqo.com` 根域，真实文章路径只留在显示文本里、且用 `›` 而非 `/` 分隔。同步脚本已兼容；但建议后续把链接 target 修正为真实 URL，否则点开只会跳首页。
2. **文章改过名**：`motion-game-console-no-subscription` 站点现名为 `motion-game-console-with-no-subscription`，靠标题模糊匹配兜底才对上。

**产出文件**
- `data/shopify/blog_posts_dates_kyniqo.csv` / `.json`（30 篇精确发布时间）
- `data/shopify/feishu_sync_audit_2026-09-23.json`（24 条修正的 old→new 审计，可回滚）
- `data/shopify/blog_posts_dates_nexplayground.*`（竞品 nexplayground.com 68 篇，顺带抓取备用）
- `tools/fetch_shopify_blog_dates.py`、`tools/sync_shopify_to_feishu.py`

---

## 3. Apify 抓取工具维护（原清单10）— 完成 ✅

本机（Mac）原先没有任何 Apify 脚本，从零搭建了编排层：

- `tools/apify/config/actors.json` — 5 个 target 的 actor id + 默认入参 + 输出路径
- `tools/apify/run_actor.py` — 通用 runner（`--list` / `--check` / 运行单个 target / 覆盖入参），无 token 时优雅退出码 2
- `tools/apify/README.md` — 配置说明 + 不需要 Apify 的替代路径

已验证：
- `--list` 正常输出 5 个 target
- 无 token → 退出码 2，不报错中断
- 假 token → `--check` 正确返回 401 并提示 token 无效

配置 token 后即可直接跑：
```bash
export APIFY_API_TOKEN=apify_api_xxx
python3 tools/apify/run_actor.py nex_meta_ads
python3 tools/apify/run_actor.py nex_facebook_page
```

---

## 4–8. 新增 5 项 — 全部占位未执行 ⏸

按授权要求「未补充前不得臆造执行」，4/5/6/7/8 保持占位，等你给具体内容。

---

## 已归档项（本次未触碰）✅

1 越南TikTok达人邀约 / 2 Vinko Pitch / 3 DDPAI视频分析 / 4 Zalo邀约 / 7 TikTok Affiliate抓取 / 8 订单物流追踪 / 9 SKU样本按Series整理 / 11 岗位职责文书 — 全部未执行。

---

## 产出清单

```
kyniqo夜间执行任务/
├── 夜间执行报告_2026-09-23.md
├── data/
│   ├── nex_playground/
│   │   ├── site_pages.json          83 页营销文案（新建 27 条入飞书）
│   │   ├── products.json  collections.json  site_summary.json
│   │   └── sitemap.xml
│   └── shopify/
│       ├── blog_posts_dates_kyniqo.csv / .json     30 篇精确发布时间
│       ├── feishu_sync_audit_2026-09-23.json       24 条修正审计
│       └── blog_posts_dates_nexplayground.*        竞品 68 篇（备用）
└── tools/
    ├── crawl_nex_site.py            官网页面爬虫（无需 Apify）
    ├── fetch_shopify_site.py        独立站商品抓取
    ├── fetch_shopify_blog_dates.py  博客发布时间抓取（可换域名）
    ├── sync_shopify_to_feishu.py    发布时间 diff + 同步
    ├── sync_nex_site_to_feishu.py   独立站内容 diff + 同步
    └── apify/{README.md, run_actor.py, config/actors.json}
```

---

## 待办 / 需要你确认

1. **Apify API Key** — 补上后 Meta 广告 + Facebook 主页两类数据才能抓。写入 `~/.zshrc` 的 `APIFY_API_TOKEN` 即可，代码无需改动。
2. **GitHub 仓库名** — 现在推到 `zonggengl-sudo/zonggengl`（该账号下唯一仓库）。若「zonggengl_sudo」是想要的新仓库名，告诉我我再建。
3. **飞书 scope** — 补 `search:docs:read` 后可直接按名字搜表格，不用绕 wiki 遍历。需要你做一次授权登录。
4. **「独立站内容」表补类目** — 加「品牌」「法务/合规」等 select 选项，可把剩余 12 个页面（about/privacy/trust 等）也纳入。
5. **表内文章链接 target 修复** — 建议批量改为真实 URL（现在点开跳首页）。
6. **新增 5 项内容** — 待你补充，补了下一晚就执行。
